# Dépendances Python de la dictée locale (versions épinglées).
# Cible : Python 3.9 (python3 d'Apple présent sur le poste managé) → wheels cp39.
# Le modèle Whisper N'EST PAS une dépendance pip : il est fourni séparément dans
# le pack (voir install_offline.sh).
# Installation hors ligne via wheels locales : voir collect_wheels.sh / install_offline.sh.
#
# Cette liste est de HAUT NIVEAU : elle sert au `pip download` (collect_wheels.sh)
# qui résout les dépendances transitives. mlx-whisper déclare `torch` en
# dépendance, mais torch (et sympy/networkx/mpmath) est INUTILE au runtime et
# volontairement exclu du kit (gain ~340 Mo). Voir DEC-0013. L'installation se
# fait ensuite en --no-deps depuis vendor/wheels (install_offline.sh).
mlx-whisper==0.4.3
mlx==0.29.3
numpy==2.0.2
sounddevice==0.5.5
pyperclip==1.11.0
rumps==0.4.0
pyobjc-framework-Quartz==11.1
